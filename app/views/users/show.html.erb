<%= @user.last_name %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <p>
          First Name: <%= @user.first_name %>
        </p>
        <p>
          Last Name: <%= @user.last_name %>
        </p>
      </h1>
    </section>
  </aside>
</div>

<%= link_to 'Create Service', :controller => :services, :action => :new, :id => @user.id %>
<%= link_to 'Show all services', :controller => :services, :action => :index %>
